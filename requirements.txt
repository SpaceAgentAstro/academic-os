# Core
# Versions use compatible-release (~=) pins for reproducible builds while
# allowing patch-level security updates. Run `pip-audit` / Dependabot in CI.
python-dotenv~=1.0
fastapi[standard]~=0.115
uvicorn[standard]~=0.30

# PDF processing
pdfplumber~=0.11
pdf2image~=1.17

# OCR
pytesseract~=0.3.13
pix2tex~=0.1.2

# Telegram
python-telegram-bot~=21.0

# Scheduling
APScheduler~=3.10

# Utilities
Pillow~=10.0

# Testing
pytest~=8.0
pytest-cov~=5.0
pytest-asyncio~=0.23
