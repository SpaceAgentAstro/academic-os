# Runtime re-audit — 2026-06-15

**Branch:** `claude/sharp-tesla-2x8dd5`
**Type:** Audit (no code remediation performed)

## What ran

- Re-verified all 19 existing `Errors/runtime.md` findings (RT-001 … RT-019)
  against the current tree.
- Live execution in a fresh venv (`fastapi`, `pydantic`, `python-dotenv`,
  `pytest`, `httpx`): initialised all DBs via `db.models.init_all_databases()`
  and exercised the GET endpoints through `TestClient`.
- Re-ran the full test suite: **20 failed, 101 passed** (unchanged).

## Result

- **All 19 prior issues remain OPEN.** None have been remediated. Headline
  defects reproduced live:
  - `GET /api/dashboard` → 500, `GET /api/briefing` → 500
    (`no such table: spaced_repetition_items`) — RT-001.
  - `pyproject.toml` entrypoint still `backend.server:app` (module absent) — RT-006.
  - CORS still hardcoded to localhost — RT-003.

## New findings added

- **RT-020 (Medium)** — `config/settings.py` import-time `DATA_DIR.mkdir(exist_ok=True)`
  omits `parents=True` and runs on import → `FileNotFoundError` startup crash on a
  nested or read-only `DATA_DIR`. Reproduced.
- **RT-021 (Low)** — `init_all_databases()` leaks SQLite connections
  (`with sqlite3.connect(...)` does not close).
- **RT-022 (Low)** — `_todays_priorities` / `get_weaknesses` cross-database N+1
  with un-indexable `LIKE` joins.
- **RT-023 (Low)** — hourly paper-scan job has no `coalesce`/`misfire_grace`/
  `max_instances` and blocking serial ingest can stack and delay the briefing.

Updated counts: Critical 2 / High 4 / Medium 8 / Low 9 (23 open total).

## Not done

- No source files were changed; this run is audit-only, as requested.
